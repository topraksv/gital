# Copy to .env and fill in from the Supabase dashboard (Project Settings → API).
# Only the publishable (anon) key ships to clients; the service-role key must
# never be here — it lives only in the GitHub Actions secrets of keepalive.yml.
EXPO_PUBLIC_SUPABASE_URL=https://nfarwwozyapjqminmyso.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=
